# Drop `yt-dlp.exe` here.

Grab the latest release from https://github.com/yt-dlp/yt-dlp/releases/latest
(file: `yt-dlp.exe`) and place it next to this file. Rebuild with
`flutter pub get && flutter build windows --release`.
