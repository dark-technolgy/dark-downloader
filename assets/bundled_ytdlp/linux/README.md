# Drop `yt-dlp` here.

Grab the latest release from https://github.com/yt-dlp/yt-dlp/releases/latest
(file: `yt-dlp_linux`) and rename it to `yt-dlp`. Ensure it is executable
(`chmod +x yt-dlp`) and rebuild with `flutter build linux --release`.
